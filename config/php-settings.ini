# Configuraciones PHP necesarias (en Dockerfile o php.ini)

# Tamaños máximos para subida de archivos
upload_max_filesize = 10M
post_max_size = 15M
max_file_uploads = 20

# Memoria y tiempo de ejecución
memory_limit = 256M
max_execution_time = 120

# Extensiones necesarias
extension=gd
extension=exif
extension=fileinfo

# Para procesamiento de imágenes grandes
max_input_vars = 3000